<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.administrator.coo">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!-- 注册设计图纸 -->
        <meta-data
            android:name="design_width"
            android:value="768" />
        <meta-data
            android:name="design_height"
            android:value="1280" />

        <activity android:name=".MainActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".ModeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".BottomPopActivity" />
        <activity android:name=".TextInputActivity" />
        <activity android:name=".FilpperActivity" />
        <activity android:name=".CountDownActivity" />
        <activity android:name=".transition.Transition1Activity" />
        <activity android:name=".transition.Transition2Activity" />
        <activity android:name=".TabActivity" />
        <activity android:name=".meterial.BottomActivity" />
        <activity android:name=".LineFeed.LineFeedActivity" />
        <activity android:name=".passwordEditText.PasswordEditTextActivity" />
        <activity android:name=".upDownListView.TestActivity"></activity>
    </application>

</manifest>